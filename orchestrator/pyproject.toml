[project]
name = "orchestrator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "taolinfeng", email = "taolinfeng@qimao.com" }
]
requires-python = ">=3.13"
dependencies = [
    "crewai>=1.15.18",
]

[project.scripts]
orchestrator = "orchestrator:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
